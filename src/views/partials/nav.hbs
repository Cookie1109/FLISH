<nav class="sticky top-0 z-50 bg-white border-b border-gray-200">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 h-14 flex items-center justify-between gap-8">
    <a href="/" class="flex items-center gap-2 shrink-0">
      <div class="w-7 h-7 rounded-lg bg-gray-900 flex items-center justify-center">
        <i data-lucide="book-open" class="w-4 h-4 text-white"></i>
      </div>
      <span class="font-bold text-gray-900" style="font-size: 1.1rem; letter-spacing: -0.3px;">
        FLISH
      </span>
    </a>

    <div class="flex items-center gap-6">
      <div class="flex items-center gap-1">
        <a href="/" class="px-3 py-1.5 rounded-lg text-sm transition-colors text-gray-500 hover:text-gray-900 hover:bg-gray-50">Trang chủ</a>
        <a href="/search" class="px-3 py-1.5 rounded-lg text-sm transition-colors text-gray-500 hover:text-gray-900 hover:bg-gray-50">Tìm kiếm</a>
        {{#if user}}
          <a href="/dashboard" class="px-3 py-1.5 rounded-lg text-sm transition-colors text-gray-500 hover:text-gray-900 hover:bg-gray-50">Dashboard</a>
        {{/if}}
      </div>
      
      {{#if user}}
      <div class="flex items-center gap-4 pl-6 border-l border-gray-200">
        <div class="flex items-center gap-2">
          <div class="w-8 h-8 rounded-full bg-gray-100 border border-gray-200 flex items-center justify-center text-gray-600 overflow-hidden shrink-0">
            {{#if user.picture}}
              <img src="{{user.picture}}" alt="Avatar" class="w-full h-full object-cover">
            {{else}}
              <i data-lucide="user" class="w-4 h-4"></i>
            {{/if}}
          </div>
          <div class="hidden sm:flex flex-col">
            {{#if user.name}}<span class="text-sm font-semibold text-gray-900 leading-none mb-1">{{user.name}}</span>{{/if}}
            <span class="text-xs text-gray-500 leading-none">{{user.email}}</span>
          </div>
        </div>
        <a href="/auth/logout" class="p-2 text-gray-400 hover:text-red-600 transition-colors bg-gray-50 hover:bg-red-50 rounded-lg" title="Đăng xuất">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
      {{else}}
      <div class="flex items-center gap-1 pl-6 border-l border-gray-200">
        <a href="/auth/login" class="px-4 py-2 rounded-lg text-sm font-medium transition-colors bg-gray-900 text-white hover:bg-gray-800">Đăng nhập</a>
      </div>
      {{/if}}
    </div>
  </div>
</nav>
